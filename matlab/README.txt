——————————
READ ME
——————————
Run populate.m first,
It prepares the matrixes needed for this exercise using the data files
It will call all the other scripts before finally returning

u, v and u.v matrixes already include in out_data folder from most recent runs
u, v matrixes from python version included in the data/python_data/ folder. Used it for visualization as a it is more accurate.

plots/ folder contains sample plots from visualization (Just Amityville for now)

New Data files should always go in the data folder under the same name that the input data files have been saved as here, to avoid code modification.